/** @jsxImportSource @builder.io/qwik */

// GENERATED BY MITOSIS

// Maybe this untyped imports shoud be generated by the compiler?
import { Fragment, component$, h, useStore, useStylesScoped$ } from '@builder.io/qwik';

export const MyComponent = component$((props: any) => {
  useStylesScoped$(STYLES);
  // mitosis shouldn't cast "any" types, let the framework infer the type
  const state = useStore({ name: 'Steve' });
  return (
    <div>
      <input
        // in qwik's case when using "useStylesScoped$" the class name is generated
        // adding the component name as a prefix might be overkill
        class="input-MyComponent"
        value={state.name}
        onChange$={(event) =>
          // this should never be null, right?
          (state.name = event.currentTarget.value)
        }
      ></input>
      Hello! I can run in React, Vue, Solid, or Liquid!
    </div>
  );
});
export default MyComponent;
export const STYLES = `
.input-MyComponent {
  color: red;
}`;
